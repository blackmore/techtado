document.observe('dom:loaded', function (event) {
	$('videos').down('div.remove_video').hide();
});

function do_this (argument) {

	//var video = $('videos').childElements();
	//	var tt = video.first()
	//		var string = tt.innerHTML;
	//		alert(string)
}