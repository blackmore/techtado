<% if current_user && !@task.freezed? %>
	<%= link_to_remote '', :html => {:class => get_status(@task)}, :url => update_show_page_path(@task, :status => "finished"	)%><%= urgent?(@task) %>
<% else %>
	<span class="<%= get_status(@task) %>"></span><%= urgent?(@task) %>
<% end %>
<% show_description(@task) %>
<span class="tsk_name"><%= @headding %></span>
<%= change_status_link(@task) %>
<p><%= @description %></p>
